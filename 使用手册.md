# SMR 社交媒体研究助手 · 使用手册

> B站视频数据采集与 AI 分析一体化工具  
> **零 npm 依赖**（纯 Node.js 内置模块），Node ≥ 20 直接运行

---

## 一、安装与部署

### 1.1 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11（其他平台可通过 `node src/main.js` 直接启动） |
| Node.js | v20 或更新版本 |
| ffmpeg（可选） | 视频+音频合并为 mp4，以及无官方字幕时的语音转写，都需要 ffmpeg |
| yt-dlp（可选） | YouTube 下片与字幕正文；放到 `tools/yt-dlp.exe` 或配置 `youtube.ytdlpPath`。不打进仓库 |

### 1.2 一键安装（Windows）

1. 解压 `SMR-Research-Assistant.zip` 到任意目录（建议纯英文路径，如 `D:\smr`）
2. 双击 **`install.bat`**
3. 脚本会自动：
   - 检测 Node.js 是否已安装（未装则提供[官网下载](https://nodejs.org/en/download)或 winget 一键安装）
   - 在桌面创建 **"SMR Research Assistant"** 快捷方式
   - 静默启动服务并自动打开浏览器
4. 浏览器打开后进入**首次配置向导**（见第二节）

> 以后使用：双击桌面快捷方式即可启动（无黑窗口，后台运行）

### 1.3 手动启动

```bash
# 解压后进入该目录（不要套一层 smr 文件夹）
cd SMR-Research-Assistant
node src/main.js
# 服务启动后自动打开浏览器 http://127.0.0.1:39010
```

---

## 二、首次配置（模型配置向导）

首次打开网页时，会自动弹出**分步配置向导**。点击顶部 ⚙️ 按钮可随时重新配置。

### 步骤 ①：文本模型

配置用于评论/弹幕分析、综合报告、知识点提炼等的**文本处理 LLM**。

1. **选择提供商**（快速填充 API 地址）：
   - DeepSeek / OpenAI / 智谱 GLM / Kimi / 通义千问 / 硅基流动 / 自定义
2. **API 地址**：自动填入，或手动输入（以 `/v1` 结尾的 OpenAI 兼容地址）
3. **API Key**：填入你的 API 密钥
4. **模型名称**：如 `deepseek-v4-flash`、`qwen-plus`、`glm-4` 等

### 步骤 ②：视觉模型

配置用于图片识别、关键帧分析等的**多模态视觉模型**。

- 勾选 **"与文本模型相同"**（默认）→ URL / Key 均跟随文本模型
- 如需独立配置（如使用不同提供商），取消勾选后分别填写视觉模型的 URL、Key
- **模型名称**：如 `glm-5.3-flash`、`deepseek-vl`、`qwen-vl-max` 等

> 💡 识图模型消费较高，顶部有 **👁 识图模型** 开关可随时禁用（禁用后关键帧分析跳过画面识别，仅定位+提帧）

### 步骤 ③：平台凭据（可选）

建议提供但不强制。配置后可采集完整评论、字幕、AI 总结等数据。

**B 站 Cookie 获取方式**：
1. 浏览器打开 [bilibili.com](https://www.bilibili.com) 并登录
2. 按 **F12** 打开开发者工具
3. 切换到 **网络** 选项卡
4. 随便点击一个请求 → 找到请求头中的 **Cookie** 字段
5. 复制**整段** Cookie 值粘贴到输入框

> Cookie 通常有效期数月，过期时会提示更新。同一页还可选填 **YouTube Data API Key**（采集 YouTube 评论/元数据需要）。

### 步骤 ④：测试与保存

- 点击 **"测试"** 按钮验证文本/视觉模型的连通性（真实调用 API，显示延迟和模型名）
- 确认无误后点击 **"保存配置"** → 页面自动刷新，配置即刻生效

---

## 三、采集功能

### 3.1 支持的输入类型

在网页输入框中粘贴以下任意链接：

| 输入类型 | 示例 |
|----------|------|
| 单个视频 | `BV1xx411c7mD` 或 `https://www.bilibili.com/video/BV1xx...` |
| UP主全部视频 | `https://space.bilibili.com/12345/upload/video` |
| 关键词搜索 | `https://search.bilibili.com/video?keyword=摄影` |
| 热门视频 | `https://www.bilibili.com/v/popular/` |
| 每周必看 | `https://www.bilibili.com/v/popular/weekly?num=1` |
| 首页推荐流 | `https://www.bilibili.com/video/recommend`（需登录 Cookie） |
| 历史记录 | `https://www.bilibili.com/history`（需登录 Cookie） |
| **合集** | `https://space.bilibili.com/14229967/channel/collectiondetail?sid=1533671` |
| YouTube 视频 | `https://www.youtube.com/watch?v=...`、`https://youtu.be/...` 或 11 位视频 ID（需 Google API Key；评论/元数据始终可采，下片需本机 `yt-dlp`） |
| YouTube 播放列表 / 频道 | `playlist?list=PLxxx`、`youtube.com/@handle`、`/channel/UCxxx`（串行采子视频，数量受「批量数量」限制） |

### 3.2 高级选项

| 选项 | 说明 | 默认值 |
|------|------|--------|
| 弹幕上限 | 每个视频最多采集多少条弹幕 | 2000 |
| 评论上限 | 每个视频最多采集多少条评论 | 1000 |
| 楼中楼/楼 | 每条主评论最多采集多少条楼中楼 | 20 |
| 批量数量 | 批量采集时最多处理多少个视频 | 5 |
| 下载清晰度 | 240P / 480P / 720P / 1080P（默认）/ 最高可用 / 最低 | 1080P |
| 合集板块 | 按板块过滤（如"导数""函数""三角函数"） | 空（全部） |
| 合集偏移 | 从合集列表第 N 条开始（配合批量数量分批续传） | 0 |
| 下载视频/音频 | 是否下载视频和音频文件 | 勾选 |
| 无官方字幕时语音转写 | 没有 B 站字幕时，用本地 whisper.cpp 或转写 API 识别音轨 | 勾选 |
| 跳过评论/字幕/AI总结 | 跳过对应数据采集 | 未勾选 |

### 3.3 合集下载

粘贴合集链接后，支持按板块分批下载：

```
# Web UI：粘贴合集链接 + 高级选项"合集板块"填板块名
# CLI：--section 板块名 --limit 数量
node src/main.js --crawl "https://space.bilibili.com/14229967/channel/collectiondetail?sid=1533671" \
  --section 导数 --limit 5 --offset 0
```

- `--section`：按板块名称过滤（如"导数""函数""数列"）
- `--limit`：本次下载数量
- `--offset`：从合集（或板块）列表的第 N 条开始，再取 `--limit` 条；窗口内已采集的视频仍会自动跳过
- 已采集过的视频自动跳过（防重复下载）
- 多分 P 稿件会**串行采集全部 P**：第 1 P 仍写在任务根目录（`current.mp4`、`subtitles.json`、`bilibili_danmaku.json`），第 2 P 起在 `p02/`、`p03/` …。评论和 AI 总结仍按稿件采一次。综合分析会拼接各 P 字幕；关键帧仍只用第 1 P。

例：第一次 `--offset 0 --limit 5`，第二次 `--offset 5 --limit 5`（不要把 offset 理解成「剩余未下载列表」）。

### 3.4 实时进度

采集过程中，页面顶部实时显示：
- 当前阶段（详情 / 弹幕 / 评论 / 字幕 / AI总结 / 下载 / 合并）
- 百分比进度条
- 已采集条数/总量

---

## 四、AI 分析功能

### 4.2 网页图片分析

采集卡下方可选图片文件 + 提示词，调用视觉模型（需已配置）。请求体为 data URL，上限约 5MB。CLI 仍可用 `--image`。

在任务详情页点击 **"LLM分析"** 选项卡：
- 选择类型：仅评论 / 仅弹幕 / **综合（视频+弹幕+评论）**
- 点击"开始 LLM 分析"

**综合分析**（推荐）会将视频信息、字幕、AI总结、弹幕、评论一起分析，输出：
- 总体概述 / 关键词 / 情绪占比
- 观众评价 / 争议点 / 焦点话题
- 弹幕与评论的关注点差异
- 给创作者/研究者的建议

分析结果自动保存到任务目录的 `analysis.json`，可复用。

### 4.2 关键帧分析

> ⚠️ 需要识图模型启用

在任务详情页点击 **"🎯 关键帧分析"** 按钮。两阶段 AI 流水线：

1. **阶段 1（deepseek-v4-flash 语义定位）**：分析弹幕+字幕，找出观众标记为"重点/高能/截图"的关键时间点
2. **阶段 2（识图模型画面识别）**：提取该时间点附近连续 5 帧画面，传给视觉模型精准识别

输出：时间点 + 触发弹幕 + 画面描述 + 屏幕文字逐字转录 + "为何值得截图"

### 4.3 合集知识报告

对合集批量任务生成 AI 学习报告：

```bash
node src/main.js --report <合集任务Id> --questions 8
```

输出 `collection_report.json`：
- **知识点地图**：按知识板块组织，关联对应视频
- **高考风格出题**（选择/填空/解答，含答案与详细解析）
- **学习路径建议**
- **易错提醒**

> 💡 出题由 LLM 生成，数学细节建议人工复核

### 4.4 图片分析

```bash
node src/main.js --image 图片.png --prompt "描述这张图片"
```

支持 PNG/JPG/WebP/GIF 格式，使用视觉模型识别。

---

## 五、任务管理

### 5.1 任务列表

页面中部的 **"🗂️ 任务归档"** 区域展示所有任务：
- 状态标签（finished / running / stale / failed）
- 创建时间、文件数量
- 运行中的任务显示实时进度条
- 每项右侧：🔄 重新采集 / 🗑 删除

### 5.2 任务详情

点击任务标题进入详情弹层，包含多个选项卡：

| 选项卡 | 内容 |
|--------|------|
| 总览 | 视频封面/播放器 + 基础信息 |
| 详情 | 视频完整 JSON 数据 |
| 评论 | 评论列表（含楼中楼） |
| 弹幕 | 弹幕列表（时间+内容） |
| 字幕 | 官方/AI 字幕，或本地/API 语音转写结果 |
| AI总结 | B站平台 AI 总结 |
| 词频 | 高频词条形图 + 时间分布图（评论按天/弹幕按视频进度） |
| LLM分析 | AI 分析（评论/弹幕/综合/关键帧） |

### 5.3 数据导出

任务详情页顶部导出栏：
- **评论 CSV / Excel**
- **弹幕 CSV / Excel**

CSV 文件使用 UTF-8 BOM 编码，Excel 可直接打开无乱码。

### 5.4 重新采集与删除

- **🔄 重新采集**：清空完成/失败标记并重新执行（已下载的视频/音频自动跳过）
- **🗑 删除**：连同整个任务目录一起删除，不可恢复（运行中的任务拒绝删除）

---

## 六、设置与配置

### 6.1 模型设置

点击顶部 **⚙️ 设置** 按钮，打开配置向导（与首次使用相同）。

配置项：
- **文本模型**：API 地址 + API Key + 模型名（用于文本分析）
- **视觉模型**：可独立配置 URL/Key/模型名，或跟随文本模型
- **B站 Cookie**：整段粘贴
- **语音转写**：无官方字幕时，用本地 whisper.cpp 或兼容 `/v1/audio/transcriptions` 的接口（见 6.4）

### 6.2 视觉模型开关

顶部 **👁 识图模型** 按钮：
- 🟢 启用中：关键帧分析、图片分析正常工作
- 🔴 已禁用：跳过视觉识别（仅定位+提帧），节省消费

### 6.3 配置文件

所有配置保存在 `config.json`（运行时可通过网页"设置"修改）：

```json
{
  "server": { "host": "127.0.0.1", "port": 39010 },
  "cookies": { "bilibili": "你的cookie..." },
  "llm": {
    "baseUrl": "https://api.deepseek.com/v1",
    "apiKey": "sk-...",
    "model": "deepseek-v4-flash",
    "reasoningEffort": "low",
    "vision": {
      "baseUrl": "",
      "apiKey": "",
      "model": "glm-5.3-flash"
    },
    "visionEnabled": true,
    "maxTokens": 2000
  },
  "asr": {
    "enabled": true,
    "engine": "auto",
    "language": "zh",
    "followLlm": true,
    "model": "",
    "whisperBin": "",
    "whisperModel": ""
  },
  "crawl": {
    "bilibili": {
      "quality": "1080p",
      "danmakuCount": 2000,
      "commentCount": 1000
    }
  }
}
```

`llm.reasoningEffort` 是思考强度（none/low/high/max），发给支持 `reasoning_effort` 的服务商（如 Ollama Pro）。关闭/浅思考更快更省，`max` 最强但最慢。只对文本分析生效，视觉调用不带。

环境变量（可选，优先级高于 config.json）：
- `SMR_LLM_BASE_URL` / `SMR_LLM_API_KEY` / `SMR_LLM_MODEL`
- `SMR_LLM_VISION_BASE_URL` / `SMR_LLM_VISION_API_KEY` / `SMR_LLM_VISION_MODEL`
- `SMR_BILI_COOKIES` / `SMR_YOUTUBE_API_KEY`
- `SMR_ASR_ENABLED` / `SMR_ASR_ENGINE` / `SMR_ASR_MODEL` / `SMR_ASR_BASE_URL` / `SMR_ASR_API_KEY`
- `SMR_WHISPER_BIN` / `SMR_WHISPER_MODEL`
- `SMR_LOCAL_TOKEN`（覆盖本机访问令牌，不写回磁盘）

### 6.4 无官方字幕时的语音转写

B 站多数视频没有官方/AI 字幕。采集流程是：

1. 先请求 B 站字幕接口（人工中文 > AI 中文 > 其他）
2. 若没有，且未勾选「跳过字幕」，则对已下载音轨做语音识别
3. 写出同样的 `subtitles.json`（`source: "asr"`），合集报告 / 综合分析 / 关键帧可直接用

**不要把模型打进 zip。** 在设置向导第 4 步的「选择模型」下拉里挑选，云端和本地是两组独立选项。

**云端（走 `/v1/audio/transcriptions`，音频会上传到对应服务商）**

| 选项 | 模型 | 说明 |
|------|------|------|
| 硅基流动 SenseVoice Small | `FunAudioLLM/SenseVoiceSmall` | 中文/粤语首选，≤50MB / 1 小时 |
| 硅基流动 TeleSpeechASR | `TeleAI/TeleSpeechASR` | 同一接口 |
| Groq Whisper Turbo / Large V3 | `whisper-large-v3-turbo` / `whisper-large-v3` | 快、带时间戳；需 Groq Key |
| OpenAI whisper-1 / gpt-4o-transcribe | `whisper-1` 等 | 官方 API；需 OpenAI Key |

DeepSeek **没有**语音转写接口，不能把聊天 Key 拿去转写。文本模型若已是硅基流动，选 SenseVoice 可跟随同一把 Key。

**本地（音频不离开本机）**

| 选项 | 体积 | 说明 |
|------|------|------|
| 本机 FunASR / SenseVoice 服务（已部署） | 约 1GB 模型 | 双击 `tools\启动FunASR.bat` 启动，监听 `http://127.0.0.1:8600/v1`（模型填 `sensevoice`）。SenseVoice 语言自动检测，中/日/英/韩/粤语混合输入均可；中文明显优于 Whisper。服务没开时转写会失败。 |
| whisper.cpp（本仓库 tools/ 已内置 CUDA 版） | 163MB~1.5GB | `tools/whisper-cli.exe` + `ggml-small-q5_1.bin` 已就绪（RTX 显卡自动 GPU 加速）。完全离线，无需常驻服务；语言跟随设置向导第 4 步的「语言」（中文/英文/日文）。 |
| 其他 OpenAI 兼容本机服务 | 需自建 | faster-whisper / Xinference 等 |

阿里云百炼 Qwen-ASR / Fun-ASR 是异步+公网文件 URL，协议不同，暂未做成一键预设。

已采过的任务可补转写：网页任务详情 → 字幕 →「语音转写补字幕」，或：

```bash
node src/main.js --transcribe <任务Id>
node src/main.js --transcribe <任务Id> --force   # 覆盖已有官方字幕
```

---

## 七、MCP 服务器

启动后提供 MCP 接口：`http://127.0.0.1:39010/mcp`

本机访问令牌在 `config.json` 的 `server.localToken`（首次启动自动生成）。Web UI 会自动携带。MCP / curl 需要：

```
Authorization: Bearer <localToken>
# 或
http://127.0.0.1:39010/mcp?token=<localToken>
```

环境变量 `SMR_LOCAL_TOKEN` 可覆盖令牌（不写回磁盘）。

11 个工具供 AI Agent 调用：
- `crawl_bilibili` / `crawl_bilibili_search` / `crawl_bilibili_uploader`
- `crawl_bilibili_popular` / `crawl_bilibili_weekly` / `crawl_bilibili_recommend`
- `crawl_youtube` / `list_archives` / `get_task_data` / `analyze_task` / `transcribe_task`

---

## 八、命令行参考

```bash
# 启动服务（同桌面快捷方式）
node src/main.js

# 采集
node src/main.js --crawl "BV号或B站/YouTube链接"
node src/main.js --crawl "BV号" --quality 720p --max-comments 500
node src/main.js --crawl "合集链接" --section 导数 --limit 5 --offset 0

# AI 分析
node src/main.js --analyze <任务Id> all        # 综合分析（视频+弹幕+评论）
node src/main.js --analyze <任务Id> comments   # 仅评论
node src/main.js --analyze <任务Id> danmaku    # 仅弹幕

# 合集报告
node src/main.js --report <合集任务Id> --questions 8

# 语音转写字幕（无官方字幕的已采任务）
node src/main.js --transcribe <任务Id>
node src/main.js --transcribe <任务Id> --force

# 关键帧分析
node src/main.js --keyframes <任务Id> --top 5 --frames 5

# 图片分析
node src/main.js --image 图片.png --prompt "描述内容"

# 剪贴板分析
node src/main.js --clipboard "要分析的文本内容"

# 任务管理
node src/main.js --list                        # 列出所有任务
node src/main.js --delete <任务Id>             # 删除任务
node src/main.js --status                      # 查看配置摘要

# 导出
node src/main.js --export <任务Id> comments csv
node src/main.js --export <任务Id> danmaku xls

# 视觉模型开关
node src/main.js --vision on                   # 启用
node src/main.js --vision off                  # 禁用
```

---

## 九、数据存储

```
data/
├── <视频标题>-<aid>/           # 单个视频任务
│   ├── bilibili_detail.json    # 视频详情（标题/UP主/播放量/简介...）
│   ├── bilibili_comment.json   # 评论（含楼中楼，按赞数排序）
│   ├── bilibili_danmaku.json   # 弹幕（时间点/内容/发送者）
│   ├── bilibili_summary.json   # B站 AI 总结（大纲/章节/摘要）
│   ├── subtitles.json          # 字幕（官方/AI，或 source=asr 的语音转写）
│   ├── bilibili_all.json       # 聚合总览
│   ├── current.mp4             # 视频+音频合并（DASH 下载 + ffmpeg）
│   ├── cover.jpg               # 视频封面图
│   ├── analysis.json           # LLM 分析结果（自动保存）
│   ├── keyframes.json          # 关键帧分析结果
│   ├── keyframes/              # 关键帧画面（每点 5 帧）
│   │   └── kf_0006_1.jpg ...
│   └── .task-completed         # 完成标记
├── collection-<sid>-batch-*/   # 合集批量任务
│   ├── bilibili_all.json       # 合集元数据 + 子任务结果
│   └── collection_report.json  # 合集 AI 报告（知识地图/出题/学习路径）
└── search-<keyword>-batch-*/   # 搜索批量任务
```

---

## 十、常见问题

### Q: 视频没有 B 站官方字幕，采集后字幕是空的？
多数视频确实没有平台字幕。需要配置语音转写（设置向导第 4 步，或 `config.json` 的 `asr`）：本地 `whisper.cpp`，或兼容 `/v1/audio/transcriptions` 的接口。采集时默认会在无官方字幕时自动转写；已有任务可在「字幕」页点「语音转写补字幕」，或 `node src/main.js --transcribe <任务Id>`。转写需要 ffmpeg 以及已下载的音频/视频。

### Q: 端口 39010 被占用怎么办？
修改 `config.json` 中的 `server.port` 字段，或用 `node src/main.js --status` 查看当前配置。

### Q: 视频下载后无法播放/没有声音？
需要系统安装 [ffmpeg](https://ffmpeg.org/download.html)。安装后 ffmpeg 需在系统 PATH 中，或在 `config.json` 的 `crawl.ffmpegPath` 指定完整路径。无 ffmpeg 时保留 m4s 分片文件（可用 VLC 播放）。

### Q: B站 Cookie 过期了？
采集时会提示"B站登录态已失效（-101）"。重新登录 bilibili.com，用 F12 复制最新 Cookie，粘贴到网页"设置"中即可。

### Q: 下载清晰度不是 1080P？
默认 1080P。如需调整：网页高级选项"下载清晰度"下拉框，或 CLI `--quality 720p`，或修改 `config.json` 的 `crawl.bilibili.quality`。

### Q: 批量采集太慢？
批量任务为异步串行队列（一次处理一个视频），可在网页"高级选项"调低弹幕/评论上限减少单个视频耗时。

### Q: 合集太大怎么分批？
使用 `--section 板块名 --limit 5 --offset 0` 分板块+分批下载。下次从 `--offset 5` 继续（offset 是合集列表下标，不是「还没下的第 N 个」）。

### Q: 接口返回 401「需要本机访问令牌」？
Web UI 会自动带令牌。curl / MCP 请使用 `config.json` 里的 `server.localToken`，放到 `X-SMR-Token` 头、`Authorization: Bearer` 或 `?token=`。

### Q: YouTube 采集失败？
需要在设置向导填写 Google API Key，或配置 `youtube.apiKey` / 环境变量 `SMR_YOUTUBE_API_KEY`。可粘贴 `youtube.com` / `youtu.be` / `music.youtube.com` 链接，或 11 位视频 ID。播单与频道按「批量数量」串行采集子视频。

下片依赖本机 **yt-dlp**（与 ffmpeg、whisper-cli 同类，不打进仓库）：放到 `tools/yt-dlp.exe`（或 PATH），也可在 `config.json` 写 `youtube.ytdlpPath`。勾选「下载视频」后任务目录会出现 `current.mp4`（或音频）以及从 `--write-sub` 转出的 `subtitles.json`。找不到 yt-dlp 时仍会采元数据+评论，日志写「未找到 yt-dlp，跳过下片」。

### Q: LLM 分析报错"未配置"？
点击顶部 ⚙️ 设置按钮，按向导填写文本模型（API 地址 + Key + 模型名）。

### Q: 视觉模型消费太高？
点击顶部 **👁 识图模型** 按钮禁用。禁用后关键帧分析只定位+提帧，不调用视觉模型。

### Q: 数据存在哪里？如何备份？
所有数据在 `data/` 目录，配置在 `config.json`。备份这两个即可。

---

## 十一、技术架构

```
前端（Web UI）    ← HTTP/SSE →    后端（Node.js 内置 http）
     │                                    │
     ├─ 采集表单                   ├─ 采集引擎（B站/YouTube）
     ├─ 实时日志流                 ├─ 任务队列系统
     ├─ 任务列表/详情              ├─ LLM 分析引擎
     ├─ 数据浏览/导出              ├─ 关键帧分析
     ├─ 词频/时间分布图            ├─ 合集报告生成
     └─ 设置向导                   └─ MCP 服务器
```

- **本机防护**：`/api` 与 `/mcp` 校验回环 Host/Origin + `server.localToken`
- **零 npm 依赖**：纯 Node.js 内置模块（http/fs/path/crypto/zlib）
- **B站签名**：WBI 签名（`MIXIN_KEY_ENC_TAB` 置换表 + MD5）
- **弹幕解码**：手写 protobuf 解码（`DmSegMobileReply`）
- **下载**：DASH 流 → m4s 分片 → ffmpeg 合并 mp4（支持 backupUrl 回退+重试）
- **请求节奏**：每请求随机延迟（bilibili 300±400ms），429 自动指数退避
